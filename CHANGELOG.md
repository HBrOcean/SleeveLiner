# 更新日志

> 项目原名 `music_enrich`，现更名为 **SleeveLiner**（取自 sleeve 唱片封套 + liner notes 唱片内页文案）。

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## v2.3.1
- 🐛 **修复 Windows 控制台崩溃**：在 cp1252/GBK 等编码的终端（如老版 cmd、CI 环境）打印中文或
  ✓/✗ 符号时抛 `UnicodeEncodeError` 导致程序退出；现改为将无法编码的字符替换为 `?`，
  不再崩溃（CI 冒烟测试同时设置 `PYTHONIOENCODING=utf-8`）

## v2.3
- ✨ 宽松二次匹配：主链路严格匹配失败后，自动用**纯歌名重搜 + 歌名/时长硬约束、歌手加分**再试一次，命中标注「宽松匹配」
- ✨ 补全**年份 / 音轨号**（来自网易云，优先取同专辑版本，避免拿到翻唱/合辑信息）
- ✨ 歌词新增**酷我**第三源兜底
- ✨ `.lrc` 编码可选（`--lrc-encoding gbk`，兼容车载 / 老播放器）
- ✨ 封面本地缓存、并发实时进度、结束汇总未成功项
- ✨ Ctrl+C 中断时保存缓存；多歌手分隔符规范化（`_` → `/`）
- 🔧 网易云请求带 Referer；输出目录保护避免误伤同名子目录
- 🐛 修复 `--rename` 后显示的输出路径不正确的问题

## v2.2
- ✨ 新增输出目录机制：原始文件不动，结果（音频副本 + `.lrc`）统一输出到各根目录下的 `output/`（`--outdir` 可自定义，递归时保留子目录结构）
- ✨ 已具备封面/歌词的文件直接复制到输出目录，不再重复联网

## v2.1（重要修复）
- 🐛 修复封面错误：旧版 `imgessl/stdmusic/{hash}.jpg` 返回的是酷狗**通用默认占位图**，改用搜索结果里的 `trans_param.union_cover` **真实封面**
- 🐛 自动识别文件中已嵌入的默认图，视为无封面重新补
- ✨ 封面取优（酷狗 800/400 取清晰者）；封面跨源兜底（酷狗失败查网易云）
- ✨ 网易云封面提升到 800×800；缓存加版本前缀

## v2.0
- ✨ 新增格式：M4A / OPUS / OGG
- ✨ 匹配增强：简繁转换、歌手别名、更多版本噪声词、网易云多源交叉印证
- ✨ 新增 `--rename`（规范为「歌名 - 歌手」，`.lrc` 同步跟随）
- ✨ 新增并发（`--jobs`）与本地缓存
- 🔧 语言标签动态判断、歌词私有行清洗、FLAC/OGG 双写歌词字段、SSL 精细化

## v1.0
- 🎉 初版：MP3 / FLAC 的封面与歌词联网补齐
